import { supabase } from "@/lib/supabase-browser";
import { getClientCafeId } from "@/lib/cafe-context-client";
import { ServiceRequest, ServiceType } from "../types/service";
export async function getServiceRequests():Promise<ServiceRequest[]>{const cafeId=await getClientCafeId();const {data,error}=await supabase.from("service_requests").select("*").eq("cafe_id",cafeId).order("created_at",{ascending:false});if(error)throw error;return(data??[]).map((item:any)=>({id:item.id,tableNumber:item.table_number,type:item.service_type as ServiceType,status:item.status,createdAt:item.created_at}));}
export async function createServiceRequest(tableNumber:number,type:ServiceType):Promise<ServiceRequest>{const cafeId=await getClientCafeId();const {data,error}=await supabase.from("service_requests").insert({cafe_id:cafeId,table_number:tableNumber,service_type:type,status:"pending"}).select().single();if(error)throw error;return{id:data.id,tableNumber:data.table_number,type:data.service_type as ServiceType,status:data.status,createdAt:data.created_at};}
export async function completeServiceRequest(id:string):Promise<void>{const cafeId=await getClientCafeId();const {error}=await supabase.from("service_requests").update({status:"completed",completed_at:new Date().toISOString()}).eq("id",id).eq("cafe_id",cafeId);if(error)throw error;}
